# 給与台帳アプリ 対応記録

修正・問い合わせ対応のたびにここへ追記する。開発PCが変わっても、この記録とリポジトリ一式があれば
経緯を追えるようにするための、蓄積型のログ。書式は自由だが、最低限「日付・症状・原因・対応」を書く。

新しく上に追記していく(新しいものが上)。

---

## 2026-09-14: 給与明細の代理登録のユーザー選択をユーザー管理と統一、モーダルのスクロール漏れも修正（青山碧さんより）

- **要望1**: 「給与明細の代理登録」のユーザー選択を、ユーザー管理カードと同じように
  「社員IDまたは苗字（ひらがな）」を入力する欄にし、その横に一覧ボタンを置いてほしい。
- **要望2**: 全社員一覧モーダルを表示中、一覧（グレーの部分）をスクロールすると背後のページも
  一緒にスクロールしてしまう。モーダル表示中は背景が動かないようにしてほしい。
- **対応**:
  - [給与明細アプリ.html](給与明細アプリ.html)の「給与明細の代理登録」カードの①を、
    テキスト入力（Enterで検索、`adminFindUsersByQuery`を使用）＋「ユーザー一覧」ボタン
    （全件モーダル）の組み合わせに変更。検索結果が複数件の場合も同じモーダルに一覧表示する
    ようにした(`openAdminUserListModalWithMatches()`を追加)。
  - モーダル表示中は`document.body`の`overflow`を`hidden`にし、閉じたら元に戻す
    `lockBodyScroll()`を追加。全社員一覧モーダルを開く/閉じるすべての箇所に適用し、
    背景ページが一緒にスクロールしてしまう不具合を解消した。
- **注意**: GAS側の変更は無いため再デプロイ不要です。

---

## 2026-09-14: 保守タブに「給与明細の代理登録」機能を追加（青山碧さんより）

- **要望**: 管理者の保守タブで、①ユーザー選択→②給与明細の種類を選択→③日付を選択→
  ④ファイルを選択、の手順で従業員の給与明細を登録できる機能がほしい。保守タブから登録した
  ものは、明細一覧の削除ボタンがONにならないようにしてほしい。
- **対応**:
  - [給与明細アプリ.html](給与明細アプリ.html)の保守タブに新しいカード
    「📄 給与明細の代理登録」を追加。①「ユーザーを選択」ボタン（ユーザー管理の「一覧から選択」
    と同じ全社員一覧モーダルを再利用）、②分類（DSBZ会社／その他）と種別（明細／賞与）、
    ③対象年月日、④PDF/画像ファイル、を入力して「登録する」で完了する。
  - アップロード自体は、既存の「＋ PDFを追加」機能と同じチャンクアップロードの仕組み
    (`uploadChunkStart`/`uploadChunkAppend`/`uploadChunkFinish`)をそのまま使い、
    アップロード先フォルダだけを選択した対象ユーザーのフォルダに切り替えられるよう
    `uploadFileToDrive()`に対象フォルダIDを指定できるオプションを追加した。
  - 「一覧から選択」でユーザーを選ぶ処理は、ユーザー管理カードの検索結果一覧・全社員一覧
    モーダルと共通の`renderAdminUserRows()`/`openAdminUserListModal()`を使うようにして
    整理・共通化した（選んだ後の処理内容は呼び出し側で自由に決められるようにした）。
  - **削除ボタンが出ない件について**: 実はこの機能のためにコードを追加する必要はなかった。
    明細一覧の削除ボタンは元々「Driveから同期してきた明細（`source:'drive'`）には表示しない」
    という仕様になっており、この代理登録機能は対象ユーザーのDriveフォルダへ直接ファイルを
    置くだけなので、通常のDrive同期明細と全く同じ扱いになり、自動的に削除ボタンは出ない。

---

## 2026-09-10: 一覧選択中に別の行を押せてしまう不具合を修正（青山碧さんより）

- **症状**: 一覧から1人選んだあと、データ読み込み中（`adminSearchUser`の応答待ち）に別の行を
  クリックできてしまい、途中で選択がすり替わってしまうことがある。
- **原因**: 行クリック時の処理が、読み込み中も一覧の行を操作可能なままにしていた。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`renderAdminUserRows()`で、クリックされた
  瞬間に一覧の全行を`pointer-events:none`にして操作不可にし、モーダル経由の場合はその場で
  モーダルも閉じるようにした（`onSelected()`をデータ取得の前に呼ぶよう順序変更）。

---

## 2026-09-10: 一覧からユーザーを選択したとき、検索欄にも社員IDを反映（青山碧さんより）

- **要望**: 「一覧から選択」（検索結果が複数件の場合も含む）でユーザーを選んだとき、
  「社員ID または 苗字（ひらがな）」の入力欄にも選んだ社員IDを入れてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`renderAdminUserRows()`内、行クリック時の
  処理に`admin-search-id`欄へ選択したIDをセットする1行を追加。

---

## 2026-09-10: 「一覧から選択」をモーダル表示に変更し、一覧のホバー配色も統一（青山碧さんより）

- **要望**: さきほど追加した「一覧から選択」を、カード内にそのまま表示するのではなくモーダルで
  表示してほしい。また一覧の各行はホバーしたら紫背景・白文字にしてほしい。
- **対応**:
  - [給与明細アプリ.html](給与明細アプリ.html)に新しいモーダル`#admin-user-list-modal`を追加。
    「一覧から選択」を押すとこのモーダルが開き、中で全社員一覧を表示、クリックでそのユーザーを
    選択すると自動でモーダルを閉じてユーザー管理カードに反映する。
  - 検索結果が複数件のときの一覧行描画処理と共通化し(`renderAdminUserRows()`)、
    検索結果一覧・モーダル一覧のどちらでも同じ見た目・挙動になるようにした。
  - 一覧の行(`.admin-multi-row`)に`:hover{background:var(--accent);color:#fff;}`を追加し、
    ホバー時に紫背景・白文字になるようにした（他のボタン類と統一した配色）。

---

## 2026-09-10: ユーザー管理に「一覧から選択」ボタンを追加（青山碧さんより）

- **要望**: ユーザー管理カードで、社員IDや苗字（ひらがな）を入力して検索する以外に、
  「一覧から選択」で全社員から選べるボタンを「リセット」の隣に追加してほしい。
- **対応**:
  - [GAS_CODE.gs](GAS_CODE.gs)（および[給与明細アプリ.html](給与明細アプリ.html)内の埋め込み
    コピー）に新しいアクション`adminListAllUsers`を追加。「社員一覧」と「ユーザー情報」の
    両シートを突き合わせ、全社員（削除済み含む）をID順に一覧で返す。
  - [給与明細アプリ.html](給与明細アプリ.html)のユーザー管理カードに「一覧から選択」ボタンを
    追加し、押すと検索結果と同じ一覧表示（クリックでそのユーザーを選択）が全社員分表示される
    ようにした。検索結果が複数件だった場合の一覧描画処理を`renderAdminUserMultiList()`として
    共通化し、検索・一覧選択の両方から使うようにした。
- **注意**: GAS側の変更を反映するには、管理者がApps Scriptエディタで再デプロイする必要があります。

---

## 2026-09-10: 「すべての明細を表示」も、開くときはのれん式・閉じるときは一瞬に（青山碧さんより）

- **要望**: 検索パネルと同じく、「すべての明細を表示」を押したときも、のれんが下りるように
  滑らかに開いてほしい。ただし「閉じる」を押したときは一瞬で閉じてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`#meisai-all`を、検索パネルと同じ
  「JSで実際の高さ(`scrollHeight`)を測って`max-height`をアニメーションさせる」方式
  (`.reveal-panel`)に変更。開くときは通常どおりアニメーションさせ、「閉じる」ボタンを押した
  ときだけ`.no-anim`クラスで一時的にアニメーションを無効化してから即座に高さ0に戻すことで、
  開くときはのれん式・閉じるときは瞬時、という要望通りの動きにした。

---

## 2026-09-10: 検索パネルのはみ出し、実は端末側の古いキャッシュ（Service Worker）が原因だった（青山碧さんより）

- **症状**: JSでmax-heightを測る方式に作り直した後も、リロードしても直っていなかった。
- **原因**: GitHub Pagesには正しい最新版が配信されていることを確認できたため、端末（ブラウザ／
  ホーム画面に追加したアプリ）側で古いキャッシュ（Service Worker）がいつまでも更新を検知できず、
  古いHTML・CSS・JSを表示し続けていたことが原因と判明。
- **対応**: [sw.js](sw.js)の`CACHE_NAME`を`kyuyodaicho-v3`から`kyuyodaicho-v4`に変更した。
  Service Workerの仕組み上、このファイル自体の中身が変わるとブラウザが更新を検知し、新しい
  Service Workerへの入れ替え・古いキャッシュの削除が強制的に行われる。今後アプリ側だけを更新
  しても反映されない場合は、この`CACHE_NAME`を上げることで解消できる。

---

## 2026-09-10: 検索パネルのはみ出し不具合、box-sizing修正では直らず。より確実な方式に作り直し（青山碧さんより）

- **症状**: 前回のbox-sizing修正をリロードして確認してもらったが、直っていなかった。
- **原因**: CSS Gridの行の高さ（`0fr`↔`1fr`）で開閉させる方式は理論上は正しいはずだが、
  実機・実ブラウザでの挙動が期待通りにならなかった（環境依存の可能性）。box-sizingの調整では
  解決しなかったため、根本的に別方式へ作り替えることにした。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の開閉方式を、CSS Gridの`0fr`/`1fr`から、
  「パネルを開くときに中身の実際の高さ(`scrollHeight`)をJavaScriptで測って`max-height`に
  セットし、閉じるときは`max-height:0`に戻す」という、より枯れた定番の実装方式に変更。
  `#btn-meisai-search-toggle`のクリックハンドラを書き換え。

---

## 2026-09-10: 検索パネルを閉じた状態で少しはみ出て表示されてしまう不具合を修正（青山碧さんより）

- **症状**: [スクリーンショット添付]直前に対応した検索パネルの滑らか開閉化のあと、パネルを
  閉じた状態でも「対象年月」ラベルの一部がわずかにはみ出して見えてしまう。
- **原因**: 開閉アニメーションに使ったCSS Gridの行の高さ`0fr`は、中身のdivの「内容の高さ」を
  0にするだけで、`padding`分は`box-sizing`の既定値（content-box）だとその0の外側に上乗せされて
  描画されてしまう。そのため閉じた状態でも、padding分の隙間に中の文字がわずかにはみ出していた。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`.filter-panel-inner`に
  `box-sizing:border-box;`を追加し、paddingを高さ0の内側に収めることではみ出しを解消した。

---

## 2026-09-10: 明細一覧の検索パネルの開閉を、のれんが下りるような滑らかな動きに（青山碧さんより）

- **要望**: 「直近3か月の明細」の検索アイコンを押したときに出てくる絞り込みパネルの表示が
  かくかくしている。上から下にのれんが下りるような動きにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`#meisai-filter-panel`を、これまでの
  `display:none`の瞬時切り替え（`is-hidden`）から、CSS Gridの行の高さを`0fr`↔`1fr`で
  アニメーションさせる方式（`.filter-panel`/`.filter-panel-inner`）に変更。パネルが上端を
  起点に高さ0から滑らかに広がって現れる、のれんが下りるような開閉になった。

---

## 2026-09-10: ログイン等の通信が重い（スマホで30秒以上かかることがある）問題を改善（青山碧さんより）

- **要望**: 前回のタイムアウト対策とは別に、通信そのものがスマホだと重く、30秒以上かかることが
  ある。根本的に改善してほしい。
- **原因**: GAS側で、ログインを含むほぼ全てのアクション（23箇所）が呼んでいる
  `findMasterSpreadsheetFile()`が、毎回Driveのフォルダ内をファイル名で検索してから
  「管理情報」スプレッドシートを開いていた。
  Driveのフォルダ内検索はSpreadsheetの直接操作より重く、応答が不安定になりやすいAPIのため、
  リクエストのたびにこれを行っていたことが、遅延・ばらつきの主な原因と考えられる。
  また、HTML側でもログイン処理の最初に毎回`bootstrapScriptUrl()`（GAS URLの確認通信）の完了を
  待ってから本体のログイン通信を始めていたため、条件によっては通信が直列に積み重なっていた。
- **対応**:
  - [GAS_CODE.gs](GAS_CODE.gs)（および[給与明細アプリ.html](給与明細アプリ.html)内の埋め込み
    コピー）の`findMasterSpreadsheetFile()`を、一度見つけたスプレッドシートIDをスクリプト
    プロパティに記憶し、2回目以降はID直接指定（高速）で開くように変更。フォルダ内検索は初回
    またはキャッシュが無効になった場合のみ行う。
  - [給与明細アプリ.html](給与明細アプリ.html)の`doEmployeeLogin()`で、`bootstrapScriptUrl()`の
    完了を待たずにログイン通信を開始するように変更（ブートストラップは裏側で並行して進める）。
- **注意**: GAS側の変更を反映するには、管理者がApps Scriptエディタで再デプロイする必要があります。

---

## 2026-09-10: ログインに非常に時間がかかる不具合を修正（青山碧さんより）

- **症状**: ログインしようとすると「ログインしています…」の表示のまま、ものすごく時間がかかる。
- **原因**: ログイン処理の最初に必ず実行される、GASのURLを取得するための通信（公開ドキュメントへの
  `fetch`）だけ、他の通信（45秒でタイムアウトしてフォールバックする仕組み）と違いタイムアウトが
  一切設定されていなかった。Google側の応答が詰まったタイミングでログインすると、この通信が終わる
  まで無期限に待たされ、ログイン処理全体が固まって見える状態になっていた。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`bootstrapScriptUrl()`内の通信に、既存の
  タイムアウト付きfetch関数を使うようにし、10秒で打ち切って既定のURLにフォールバックするようにした。

---

## 2026-09-10: 累計支給額カードの左右ボタンが、ホバー時に白背景×白文字で見えなくなる不具合を修正（青山碧さんより）

- **症状**: [スクリーンショット添付]累計支給額カードの年切り替え（‹›）ボタンをクリックしたあと、
  ホバーすると背景も文字も白くなり、矢印が見えなくなる。
- **原因**: ボタンをクリックしてカードが`:focus-within`状態になると、その状態専用のCSS
  (`.card:focus-within .btn.ghost{background:var(--surface);}`)がホバー時の紫背景を白背景に
  上書きしてしまう一方、文字色は09-09に追加したホバー用の白文字のままだったため、白背景・白文字の
  組み合わせになり矢印が見えなくなっていた。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)に`.card:focus-within .btn.ghost:hover{
  background:var(--accent);color:#fff;}`を追加し、フォーカス中でもホバー時は紫背景・白文字になる
  ようにした。

---

## 2026-09-09: ボタンのホバー配色の切り替わりを滑らかに（青山碧さんより）

- **要望**: さきほどのホバー配色変更（紫背景・白文字）が、パッと瞬時に切り替わるのではなく、
  じわっと滑らかに変化してほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`.btn`の`transition`に
  `background-color .25s ease, color .25s ease, border-color .25s ease`を追加し、ホバー時の
  色の切り替わりにアニメーションがかかるようにした。

---

## 2026-09-09: ボタンのホバー配色を「紫背景・白文字」に統一（青山碧さんより）

- **要望**: パスワード再発行やお問い合わせなどのモーダルにある白背景・紫文字のボタンについて、
  ホバー時に白背景のまま明るくなるだけでなく、紫背景・白文字になるようにしたい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`.btn.ghost:hover`に
  `background:var(--accent);color:#fff;border-color:var(--accent);`を追加。この種類のボタンは
  パスワード再発行モーダル・パスワード変更・GAS更新依頼など複数箇所で共通に使われているため、
  1箇所の修正でアプリ全体に反映される。

---

## 2026-09-09: 保守タブのカード順を実務の流れに合わせて並び替え（青山碧さんより）

- **要望**: 保守タブ内のカードの並び順を、一般的な管理者の使う流れ（ニュース→ユーザー追加→
  ユーザー管理→特別休暇・有給関連・休暇制度→操作ログ→GAS）に合わせたい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`view-maintenance`内のカードを
  「📢 ニュースを送信 → ➕ 新規ユーザー追加 → 👥 ユーザー管理 → 🏖️ 特別休暇・有給関連の設定 →
  📖 休暇制度の管理 → 📋 操作ログ → 🛠️ GAS更新依頼」の順に並び替え。各カードの中身・機能は変更なし。

---

## 2026-09-04: スマホ版でログイン・ログアウト時のスプラッシュ演出をOFFに（青山碧さんより）

- **要望**: 起動時・ログアウト時に出る「アイコンがふわんと出てくる」独自のスプラッシュ演出は、
  スマホ版だとOSやブラウザ側に似た演出が既にあるので、スマホ版では出さなくてよい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の起動時スプラッシュ(body直下のインライン
  script)と、ログアウト時に同じ演出を出す`showSplash()`の両方に、画面幅680px未満(既存のPC/スマホ
  切り替えと同じ基準)であれば演出なしで即座にスキップする分岐を追加。PC版(680px以上)では従来通り。

---

## 2026-09-04: 保守タブの見出しから「（管理者用）」を削除（青山碧さんより）

- **要望**: 保守タブ自体がそもそも管理者にしか表示されないので、タブ内の各見出しについている
  「（管理者用）」の文字は不要。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`view-maintenance`内、7つの見出し
  (ニュースを送信・GAS更新依頼・操作ログ・新規ユーザー追加・ユーザー管理・
  特別休暇有給関連の設定・休暇制度の管理)から「（管理者用）」を削除。設定タブ内の
  「システム設定（管理者用）」は、設定タブ自体は一般社員も開けるため、そのまま残した。
  `APP_VERSION_HTML`を`2026.09.04.16`に更新。

---

## 2026-09-04: 「設定」タブにも保守タブと同じカードデザインを適用（青山碧さんより）

- **要望**: 保守タブに施した「枠を太く・アイコン・大きく見やすい見出し」を、設定タブにも
  同様に適用してほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`#view-maintenance`向けCSSを
  `#view-settings`にも適用するよう拡張(太い枠・左の色帯・大きく紫色の見出し)。設定タブの
  各カード見出しに絵文字アイコンを追加(📱アプリとしてインストール、🔑アカウント、
  🔔お知らせ履歴、☁️Google Drive連携、💰給与の基礎情報、⚙️システム設定、
  📅支払い・有給ルール)。`APP_VERSION_HTML`を`2026.09.04.15`に更新。

---

## 2026-09-04: 「保守」タブの見出しを大きく・濃い色に（青山碧さんより）

- **要望**: 前項の見出しについて、薄い色合いではなく見やすい色合いで、もっと大きい文字にしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`#view-maintenance .section-title`を、
  文字サイズ14px→17px、色を薄いグレー(`--text-2`)からアクセントカラーの紫(`--accent`、
  左の色帯と同じ色)に変更。`APP_VERSION_HTML`を`2026.09.04.14`に更新。

---

## 2026-09-04: 「保守」タブのカードを見分けやすく（枠を太く・アイコン追加）（青山碧さんより）

- **要望**: 「保守」タブの各項目(カード)がデザイン統一のせいで見分けにくいので、枠を太くしたり
  アイコンを付けたりして、統一感を保ったまま1項目ずつ見やすくしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)
  - `#view-maintenance .card`に太め(2px)の枠と、6px幅の紫色(アクセントカラー)の左帯を追加。
    全カード共通の見た目にすることで「統一感」を保ちつつ、通常のカードより目立つようにした。
  - 各カードの見出しに絵文字アイコンを追加(📢ニュースを送信、🛠️GAS更新依頼、📋操作ログ、
    ➕新規ユーザー追加、👥ユーザー管理、🏖️特別休暇・有給関連の設定、📖休暇制度の管理)。
  - 見出しの文字サイズも13px→14pxにわずかに拡大。
  - `APP_VERSION_HTML`を`2026.09.04.13`に更新。

---

## 2026-09-04: 「保守」タブにGAS更新依頼機能を追加（青山碧さんより）

- **要望**: 管理者アカウント所持者向けに、「保守」タブでGASの更新依頼ができる項目がほしい。
  更新後のGASファイル(.gs)を添付し、更新目的を必須で入力して依頼メールを送れるようにしたい。
  送信先メールアドレスはデフォルトa_aoyama@dsbz.jpとしつつ、「設定」タブのシステム設定
  (管理者用)から変更できるようにしてほしい。
- **対応**: 既存の「お問い合わせ」機能(添付ファイル付きメール送信)や、交通費・休暇制度申請の
  受信先メール設定の仕組みと同じパターンで実装した。
  - [給与明細アプリ.html](給与明細アプリ.html)の「保守」タブに、GASファイル添付(.gs必須)・
    更新目的(必須)・送信ボタンからなる「GAS更新依頼（管理者用）」カードを追加。
    ファイルはブラウザがMIMEタイプを認識できないことがあるため、`text/plain`を明示した
    Fileオブジェクトを作り直してからアップロードするようにした。
  - 「設定」タブのシステム設定(管理者用)に、GAS更新依頼メールの受信先を設定する欄を追加。
  - [GAS_CODE.gs](GAS_CODE.gs)に`getGasUpdateEmail()`・`sendGasUpdateRequest`・
    `getGasUpdateContact`・`updateGasUpdateContact`アクションを追加(HTML内の埋め込みコピーにも同様に追加)。
  - `APP_VERSION_HTML`を`2026.09.04.12`に更新。
- **注意**: 今回の対応にはGAS_CODE.gs側の変更を含むため、**Apps Scriptの再デプロイが完了するまでは
  「依頼を送る」を押すと「unknown action」エラーになる。** 再デプロイ後に動作確認をお願いします。

---

## 2026-09-04: 累計支給額「今年」表示時、次へボタンを非表示に（青山碧さんより）

- **要望**: 「累計支給額」で今年の表示になっている時、右の「›」(次へ)ボタンは
  グレーアウトではなく非表示にしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`renderYTDTotals()`で、
  `ytd-next`ボタンに対して`disabled`属性を付ける実装から、`is-hidden`クラスを
  付け外しする実装に変更。今年を表示中はボタン自体が表示されなくなる。
  `APP_VERSION_HTML`を`2026.09.04.11`に更新。

---

## 2026-09-04: ログアウト時にも起動時と同じスプラッシュ演出を追加（青山碧さんより）

- **要望**: ログアウトした時も、ログイン時と同様にアイコンがふわっと数秒表示されるようにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)に`showSplash()`関数を追加(起動時の演出と同じ
  `.splash-screen`要素・アニメーションを、その場で作って表示→約1.3秒後にフェードアウトして自動的に
  消える)。起動時のインラインscriptは1回限りの実装で使い回せないため、同じ見た目を再現する形にした。
  「ログアウト」ボタンの処理の最後に`showSplash()`を追加。`APP_VERSION_HTML`を`2026.09.04.10`に更新。

---

## 2026-09-04: お知らせ詳細の「既読にする」ボタンを廃止し、×閉じるで既読扱いに（青山碧さんより）

- **要望**: お知らせ詳細モーダルで、右上の「×」で閉じたら既読扱いにしてほしい。下部の
  「既読にする」ボタンは廃止し、「未読のままにする」だけ残して中央寄せにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の「既読にする」ボタン(`news-detail-mark-read`)を
  HTML・JS(クリック処理、`openNewsDetail()`内の表示切り替え)から削除。「×」(`news-detail-close-x`)の
  クリック処理に`markNewsRead()`を追加し、閉じると既読になるようにした。「未読のままにする」の
  親要素(`#news-detail-read-actions`)を中央寄せ(`justify-content:center`)にし、ボタン自体も
  幅いっぱいに広がらないよう`width:auto`に変更。`APP_VERSION_HTML`を`2026.09.04.9`に更新。

---

## 2026-09-04: 確認モーダルの半日/一日選択を撤回、選択ボタンの見た目を修正（青山碧さんより）

- **要望**: ①前項で追加した確認モーダル側の半日/一日切り替えは無し(フォーム側のボタンだけでいい)に
  戻したい。②フォーム側の「一日」「半日」ボタンで、選択中(active)の見た目が分かりにくいので、
  選択中の方を紫色にしてほしい。
- **原因**: ②について調査したところ、そもそも`.daytype-btn.active`用のCSSを定義し忘れており、
  ボタンをクリックしても見た目上は何も変化していなかった(クリック自体・値の切り替えは
  正しく機能していた)。
- **対応**: 確認モーダル側の半日/一日機能(前項で追加した`showConfirmModal()`の
  `{daytype, initialDays}`オプション・`confirm-modal-daytype`要素)を削除し、`applyLeave()`も
  フォーム側の値をそのまま使う元の形に戻した。あわせて`.daytype-btn.active{background:var(--accent);
  color:#fff;border-color:var(--accent);}`を追加し、選択中のボタンが紫色で分かるようにした。
  `APP_VERSION_HTML`を`2026.09.04.8`に更新。

---

## 2026-09-04: 有給使用申請の確認モーダルにも半日/一日の切り替えを追加（青山碧さんより）

- **要望**: 前項で追加した半日/一日ボタンについて、確認ダイアログ(最終確認のポップアップ)の方にも
  切り替えを付けてほしい。
- **対応**: 共通の確認モーダル`showConfirmModal()`に、任意の第2引数`{daytype:true, initialDays:...}`を
  渡すと半日/一日ボタンを表示するオプションを追加(既存の他の呼び出し箇所は引数を渡さないので
  今まで通りボタン無しの単純な確認のまま)。選ばれた日数はモジュール変数`confirmModalDayType`に
  入るので、呼び出し側(`applyLeave()`)はモーダルを閉じた後にそれを読み、実際に消費する日数として使う。
  これにより、フォーム側で選んだ日数を確認モーダルでもその場で変更できるようになった
  (変更した場合は残日数チェックもモーダルを閉じた後の値で再チェックする)。
  `APP_VERSION_HTML`を`2026.09.04.7`に更新。

---

## 2026-09-04: 有給の単日使用申請に「半日／一日」選択を追加（青山碧さんより）

- **要望**: 「有給」タブの「この日を使用申請する」で、半日か一日かを選べるようにしてほしい。
  デフォルトは一日を選択済みの状態にし、どちらか片方を必ず選ぶ(未選択・両方選択は不可)ようにする。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の使用日入力の下に「一日」「半日」の
  トグルボタン(`.daytype-btn`)を追加。既存の「お問い合わせ」種別選択と同じ、ボタン方式の
  排他選択(常にどちらか一方だけがactiveになる)にした。`applyLeave()`の`const days = 1`(固定値)を
  選択された値(`yukyuDayType`、1または0.5)を使うように変更。確認ダイアログの文言にも
  「半日」「1日」の別を表示するようにした。有給残日数の計算・記録は元々小数(0.5等)に
  対応済みのため、追加の対応は不要だった。あわせて`APP_VERSION_HTML`を`2026.09.04.6`に更新。

---

## 2026-09-04: 通信方式をJSONP優先からfetch優先に変更（青山碧さんより）

- **背景**: PWAとしてインストールしたアプリに、意図せず個人のGoogleアカウントが紐づいてしまう
  ことがあり、毎回サインアウトして回避するのは現実的でないとの指摘。通信方式そのものを
  見直すことにした。
- **原因の再整理**: これまで何度も発生した「スクリプトの読み込みに失敗しました」は、
  scriptタグ(JSONP)での読み込みだけがGoogleのセキュリティ機能でブロックされる現象だった。
  一方、通常の`fetch`(GET)は同じ状況でも問題なく通ることを確認済み。つまり原因は
  URLの中身(パスワードの有無など)ではなく、**scriptタグという読み込み方式そのもの**にあった。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`jsonpRequest()`を書き換え、
  まず通常の`fetch`(GET)を試し、失敗した場合のみ従来のscriptタグ方式(`jsonpViaScriptTag()`として分離)に
  フォールバックするようにした。この関数はアプリ内のほぼ全通信で共通に使われているため、
  1箇所直すだけでログイン・明細一覧・PDF取得など全体に効く。あわせて、不要になった
  ログイン専用のPOST処理(`postRequest()`)と、それに伴い追加していたGAS側の`doPost`を削除した
  (元のシンプルな構成に戻した)。`APP_VERSION_HTML`を`2026.09.04.5`に更新。

---

## 2026-09-04: Drive関連の通信だけ失敗する不具合を修正（青山碧さんより）

- **症状**: ログインは成功するのに、明細一覧の読み込み(Drive関連の通信)だけ
  「スクリプトの読み込みに失敗しました」で毎回必ず失敗する。GAS再デプロイ・コード再貼り付けでも直らず。
- **原因**: [給与明細アプリ.html](給与明細アプリ.html)には2つの「GAS URL」変数があった。
  - `COMPANY_SCRIPT_URL`: ログイン等が使う。起動時に`bootstrapScriptUrl()`がブートストラップ用
    ドキュメントから最新値を取得して更新する
  - `settings.scriptUrl`: 明細一覧(`list`)・PDF取得・設定の読み書きなど、Drive関連の処理の大半が
    実際に使っているのはこちら
  - `settings.scriptUrl`はページ読み込み時に一度だけ、**まだブートストラップが完了する前の
    (フォールバックの)`COMPANY_SCRIPT_URL`の値**で固定されるだけで、ブートストラップ完了後に
    最新値へ同期する処理が無かった。ログインだけ動いてDrive関連だけ動かなくなる、という
    今回の症状と一致する。
- **対応**: `bootstrapScriptUrl()`内で、ブートストラップ完了後に`settings.scriptUrl`も
  最新の`COMPANY_SCRIPT_URL`へ同期するよう修正。あわせて`APP_VERSION_HTML`を`2026.09.04.4`に更新。

---

## 2026-09-04: 明細PDFのダウンロードボタンを追加（青山碧さんより）

- **要望**: PDFをダウンロードできるボタンを、①明細を開いたモーダルの中(左上) ②明細一覧の
  各行(「閲覧」の右隣)の両方に追加してほしい。紛失表示の行には不要。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)
  - 共通の`downloadFile(doc, label)`関数を追加。まだ読み込んでいなければ`fetchFileBytes()`で
    取得し、Blobを作って`<a download>`要素をその場で作成・クリックしてダウンロードさせる方式。
    ファイル名は明細のラベル(例:「2026年8月度の明細」)+拡張子(MIMEタイプから判定)。
  - 明細モーダルのヘッダー左側(タイトルの左)にダウンロードアイコンボタンを追加
    (`modal-download-btn`)。
  - 明細一覧の各行で、実データがある(紛失ではない)場合のみ「閲覧」タグの右隣に
    ダウンロードボタン(`.dl-btn`)を追加。
  - `APP_VERSION_HTML`を`2026.09.04.3`に更新。

---

## 2026-09-04: 給料日前表示の色分け・表記・当日の扱いを調整（青山碧さんより）

- **要望**: ①給料日前(明細未登録)の表示を緑色にしてほしい ②表記を「20〇〇年〇月度**の明細**」で
  統一してほしい ③給料日**当日**になったら、そのまま「紛失」扱いでよい ④給料日当日は、
  「次回給料日」の表示の隣に王冠マーク(👑)を出してほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)
  - `--success`/`--success-soft`という緑色の変数を追加し、`.ledger-row.pending`・`.tag.pending`を
    従来の灰色から緑色に変更(ホバー時も含む)。
  - `renderLedgerRow()`の給料日前表示のラベルを`${ym.y}年${ym.mo}月度の明細`に統一。
  - 「給料日まであと0日(＝当日)」の分岐を削除し、当日以降は他の未登録月と同じ「紛失」表示に
    一本化した。
  - `nextPayday()`の近くに`isSameDate()`・`paydayCrownSuffix()`を追加し、「次回給料日」表示
    (明細タブのサマリーの`sum-payday`、給与タブの`payday-next`の両方)で、当日が給料日の場合に
    👑を付け加えるようにした。
  - `APP_VERSION_HTML`を`2026.09.04.2`に更新。

---

## 2026-09-04: 起動時のスプラッシュ画面を追加、バージョン番号を更新（青山碧さんより）

- **要望**: アプリを開いたときに、よくあるアプリのようにアイコンがふわっと浮き出てから
  ログイン画面になるようにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`<body>`先頭に`#splash-screen`を追加。
  既存のアイコン(`<link rel="icon">`のbase64画像をそのまま再利用)を、拡大しながらふわっと
  出てくるアニメーション(`cubic-bezier`でわずかに弾む動き)で表示し、約0.9秒後にフェードアウトして
  ログイン画面が現れる。アイコン画像を別途複製せず、既存の`<link>`から実行時に読み込む形にした。
- **あわせて対応**: GASの再デプロイが完了したことを確認できたため(`checkAppVersion`が
  `notified:false`を返すようになった＝新しいコードが反映されている)、保留にしていた
  `APP_VERSION_HTML`を`2026.09.04.1`にまとめて更新した。これで、9/1にpushしていた
  「入力欄の上下キー移動」「本人確認モーダルの改善」「ログインPOST化」等の一連の変更も
  正式にバージョンとして記録される。
- **注意**: ログインのPOST化([[project-login-post-migration]]参照)は、Apps ScriptのWebアプリが
  POSTリクエストに対してCORSヘッダーを返さない仕様上の制約により、実際には機能していない
  (常に既存のJSONP方式にフォールバックしている)。動作自体は問題ないが、狙っていた
  「パスワードをURLに載せない」効果は得られていない。詳細は非公開のメモ側に記録。

---

## 2026-09-01: ログイン処理の通信方式を見直し（青山碧さんより）

- **症状**: PCによっては、ブラウザの一部のセキュリティ機能によってログイン通信が
  妨げられることがあった(Chrome・Edgeの両方で確認)。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)のログイン処理の送信方式を見直した
  (`postRequest()`を追加し、まずそちらを試みて、失敗時のみ従来方式に自動で戻るようにした)。
  あわせて[GAS_CODE.gs](GAS_CODE.gs)に`doPost`を追加。
  **注意**: この対応の効果を最大限得るには、GAS_CODE.gsの再デプロイが必要(他の保留中の
  修正と合わせて再デプロイをお願いします)。再デプロイ前でも、従来通りログインは可能。
- 技術的な詳細はセキュリティに関わるため、ここでは割愛。

---

## 2026-09-01: 入力欄で上下キーによる移動をアプリ全体に対応（青山碧さんより）

- **要望**: 何かの入力欄にフォーカスが当たっている状態で上下キーを押したら、次/前の入力欄に
  移動できるようにしてほしい。
- **経緯**: これと同じ機能自体は、管理者のユーザー編集パネル(`admin-user-panel`)だけに限定して
  既に実装されていた。今回、それをアプリ全体に一般化した。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の該当箇所を、`admin-user-panel`限定の
  keydownリスナーから、`document`全体を対象にしたリスナーに置き換え。対象は`type="text"`・
  `type="email"`・`type="password"`の入力欄のみ(`type="number"`・`type="date"`・`type="month"`は
  ブラウザ標準で上下キーが値の増減に使われるため、意図的に対象外)。移動範囲は「開いている
  モーダルの中」または「表示中のタブの中」に限定し、非表示の入力欄は対象から除外している。

---

## 2026-09-01: 「確認しています」文言の削除とEnterキー送信対応（青山碧さんより）

- **要望**: ①ローディング表示(前項)を追加したので「確認しています…」の文字表示はもう不要。
  ②「ID・パスワードを忘れた場合」モーダルで、入力後にEnterキーを押したら「本人確認する」を
  押したのと同じ扱いにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)で`btn-forgot-find`クリック時の
  `status.textContent = '確認しています…'`を削除(空文字クリアのみ残す)。また、
  `forgot-sei`・`forgot-mei`・`forgot-dob`の各入力欄にkeydownリスナーを追加し、Enterキーで
  `btn-forgot-find`をクリックするようにした(ログインID・パスワード欄と同じ実装パターン)。
  今回もバージョン番号の更新は見送り(理由は前項と同じ)。

---

## 2026-09-01: 「本人確認する」ボタンにローディング表示を追加（青山碧さんより）

- **要望**: パスワードを忘れた場合の「本人確認する」ボタンを押した際、処理中はボタンが
  ぐるぐる回るローディング状態になってほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`btn-forgot-find`クリック処理を、
  既存の`withButtonBusy()`ヘルパー(ログインボタン等で使っているのと同じ仕組み。ボタンに
  `.is-busy`クラスを付け、文字を透明にしてスピナーを重ねて表示する)でラップするよう変更。
  入力チェック(苗字・名前・生年月日の未入力)はローディングに入る前に行う。
- **注意**: 今回はバージョン番号(`APP_VERSION_HTML`)の更新を見送った。GAS_CODE.gsの
  `checkAppVersion`自動投稿の無効化がまだ本番のApps Scriptに再デプロイされておらず、
  バージョンを上げるたびに「システムが更新されました」の重複お知らせが増えてしまうため。
  再デプロイが完了したら、この分もまとめてバージョンを上げてお知らせを送る。

---

## 2026-09-01: 所定労働時間がたびたびリセットされる不具合を修正（青山碧さんより）

- **症状**: 「設定」タブで入力した所定労働時間が、しばらくすると消えて（0扱いになって）しまう。
  読み込みエラーなのか、値自体がリセットされているのか原因不明とのことで調査依頼。
- **原因**: [給与明細アプリ.html](給与明細アプリ.html)の`serializeSettingsText()`（設定をDriveへ書き出す関数）に、
  `所定労働時間:`の行を出力する処理が存在しなかった（`基本給:`など他の項目は書き出すのに、
  所定労働時間だけ抜けていた）。読み込み側の`parseSettingsText()`は`所定労働時間:`を正しく解釈
  できるようになっていたが、書き出し側が対応していなかったため、何らかの操作でDriveへ設定が
  同期されるたびに、Drive上のドキュメントからこの項目が消えていた。同じ端末のブラウザに
  ローカル保存された値が残っている間は表示上問題なく見えるが、別端末で読み込んだり
  ブラウザのデータが消えたりすると「リセットされた」ように見えていた。読み込みエラーではなく、
  保存漏れによるデータ消失。
- **対応**: `serializeSettingsText()`に`所定労働時間:${settings.standardHours}`を出力する行を追加。
  あわせて`APP_VERSION_HTML`を`2026.09.01.7`に更新。
  **注意**: この不具合が起きていた間にDriveへ同期されてしまった場合、Drive上の値は既に消えている
  可能性があるため、直しても値が0のままの社員がいれば、設定タブで再入力してもらう必要がある。

---

## 2026-09-01: 給料日前表示の行にホバーすると文字が消える不具合を修正（青山碧さんより）

- **症状**: 明細一覧で「給料日まであと〇日です」の行にマウスを乗せると、文字が白くなって
  背景に溶けて見えなくなる。
- **原因**: [給与明細アプリ.html](給与明細アプリ.html)の`.ledger-row.pending:hover`まわりで、
  行の背景色や説明文(`.info .f`)・丸印(`.dot`)の色はホバー時用に上書きしていたが、
  月度タイトル(`.info .m`)の色だけ上書きし忘れていた。そのため背景は透明のままなのに、
  全ledger-row共通のホバー規則(`.ledger-row:hover .info .m{color:#fff}`)で文字だけ白くなっていた。
- **対応**: `.ledger-row.pending:hover .info .m{color:var(--text);}`を追加。あわせて
  `APP_VERSION_HTML`を`2026.09.01.6`に更新。

---

## 2026-09-01: お知らせ一覧に一括既読機能を追加（青山碧さんより）

- **要望**: 「新着のお知らせ」の一覧を開いたときに、一括で既読にできる機能がほしい。
  🔴🟡💧の分類（重要／本アナウンス／お知らせ）ごとの一括既読と、全体の一括既読の両方に対応してほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)のお知らせモーダルに「まとめて既読にする」
  「🔴を既読にする」「🟡を既読にする」「💧を既読にする」の4つのボタンを追加(`news-read-bulk-bar`)。
  いずれも、今その一覧に表示されている(フィルターや「未読のみ」表示を適用した後の)お知らせのうち
  未読のものだけをまとめて既読にする(`markVisibleNewsRead()`)。管理者専用の「複数選択削除」モード
  とは独立していて、一般社員も含め誰でも使える(既読状態は社員ごとのローカル管理のため)。
  あわせて`APP_VERSION_HTML`を`2026.09.01.5`に更新。

---

## 2026-09-01: 累計支給額を年度(4月始まり)表示・年度切り替え対応に（青山碧さんより）

- **要望**: 「給与」タブの「今年の累計支給額」を、単に「9月度まで」ではなく
  「2026/04/01〜〇/〇/〇（今年）」のように実際の期間を表示してほしい。また、去年など
  過去の年度も「（〇年前）」という形で切り替えて見られるようにしてほしい。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)のカードに前へ／次へボタン(`ytd-prev`/`ytd-next`)を追加し、
  年度(4/1始まり〜翌3/31)を切り替えられるようにした。今年度は「開始日〜本日（今年）」、
  過去の年度は「開始日〜翌年3/31（〇年前）」の形式で表示する。集計対象も、これまでの暦年
  (1月〜)ベースから、この年度定義(4月始まり)に合わせて変更した。`renderYTDTotals()`を
  年度オフセット(`ytdYearOffset`)対応に書き換え。あわせて`APP_VERSION_HTML`を`2026.09.01.2`に更新。
  追記: 期間ラベルの文字が小さく見づらいとの指摘を受け、`.ytd-range-label`(17px・太字)を追加して
  拡大。`APP_VERSION_HTML`を`2026.09.01.3`に更新。コミット `f74c83c`。
  再追記: 「一般的な会社は1〜12月では」との指摘で再検討し、集計基準を年度(4月始まり)から
  **暦年(1/1〜12/31)** に変更し直した。日本の給与では年末調整・源泉徴収票が暦年基準なので、
  こちらの方が実態に合う。表示形式(期間ラベル・年切り替えボタン)はそのまま。
  `APP_VERSION_HTML`を`2026.09.01.4`に更新。

---

## 2026-09-01: 給料日前の月の明細が「紛失」表示になる（青山碧さんより）

- **症状**: 9/1時点（次回給料日は9/4）で、明細一覧に「2026年9月度紛失／この月の明細が登録されて
  いません」と赤い「紛失」タグ付きで表示されていた。まだ給料日が来ていないだけで、実際には
  データが失われたわけではないため、紛らわしいという指摘。
- **原因**: [給与明細アプリ.html](給与明細アプリ.html)の`renderLedgerRow()`で、該当月の明細ドキュメントが
  見つからない場合、給料日が過ぎているかどうかに関わらず一律で「紛失」表示にしていた。
- **対応**: 明細が無い月について、`getPaydayForYM(ym.y, ym.mo)`でその月度の給料日を求め、
  今日より未来なら「給料日まであと〇日です」、当日なら「本日が給料日です」という中立的な表示
  （灰色の「あと〇日」/「本日」タグ）に変更。給料日を過ぎてもまだ登録されていない場合のみ、
  従来通り赤い「紛失」表示のままにした。CSSに`.ledger-row.pending`・`.tag.pending`を追加。
  あわせて`APP_VERSION_HTML`を`2026.09.01.1`に更新。コミット `4ed5a9f`。

---

## 2026-08-31: 残業代見込みの計算が実際と違う（青山碧さんより）

- **症状**: 「給与」タブのサマリーでは「今月の残業時間：13時間」と表示されているのに、その下の
  「残業代の見込み」の計算では通常残業時間が0.5時間になっていて、金額もそれで計算されていた。
- **原因**: Driveから最新の`otHoursByMonth`を同期する処理(`pullSettingsFromDrive`)で、
  同期後の再描画時に`renderOvertime()`(見込み金額の再計算)は呼んでいたが、その計算の元になる
  `#ot-hours`入力欄自体を最新値で埋め直す`loadOvertimeHoursField()`の呼び出しが抜けていた。
  そのため入力欄は、ログイン直後・Drive同期前にこの端末のローカルストレージに残っていた古い値
  (0.5)のままになっていた。サマリー欄は`otHoursByMonth`を都度直接参照するので常に最新表示だった。
- **対応**: [給与明細アプリ.html](給与明細アプリ.html)の`pullSettingsFromDrive()`内、
  `renderOvertime()`呼び出しの直前に`loadOvertimeHoursField()`を追加。あわせて
  `APP_VERSION_HTML`を`2026.08.31.1`に更新。コミット `9200dd6`。

---

## 2026-08-31: PC移行後、ログインだけ失敗する（青山碧さんより）

- **症状**: 給与台帳アプリ(`https://debztest.github.io/DSBZ-/給与明細アプリ.html`)で、
  社員ID・パスワードを入力してログインボタンを押すと「ログインに失敗しました：スクリプトの
  読み込みに失敗しました（URLを確認してください）」と表示される。DevToolsのNetworkタブでは、
  `exec?action=employeeLogin...`が302リダイレクト後、リダイレクト先
  (`script.googleusercontent.com/macros/echo?...`)が`net::ERR_BLOCKED_BY_CLIENT`で失敗していた。
- **切り分けで否定できたもの**: GAS/Googleアカウント側の権限（別経路からのアクセスは成功）、
  社内ネットワーク・UTM/ファイアウォール（同一ネットワークの他PC・スマホ回線・Edgeでは成功）、
  Windows Defender（AV・ネットワーク保護・ファイアウォールルール・保護履歴）、OEM/サードパーティ製
  セキュリティソフト、プロキシ設定・hostsファイル、Chrome拡張機能・企業ポリシー、Service Worker、
  Chromeのセーフブラウジング設定、Googleの高度な保護機能プログラム。
- **真の原因**: 症状が出ていたChromeの通常ウィンドウは、個人のGoogleアカウントに
  サインインした状態だった。このアカウントからサインアウトしたら
  即座に直った。ちょうどGASのURLを新しいGoogleアカウントのデプロイに切り替えた直後で、
  そのURL自体がGoogle上でまだ実績（信頼スコア）が浅かった上、ログインリクエストが
  `&password=...`とURLパラメータでパスワードを送る作りになっており、フィッシングパターンに
  類似していたため、サインイン中のアカウントに紐づく何らかのリアルタイム保護に検知された
  可能性が高い（シークレットウィンドウ・サインアウト後のChrome・Edgeでは全て成功したため）。
- **対応**: このアプリを開くブラウザでは個人のGoogleアカウントにサインインしない、または
  シークレットウィンドウ／Edge等の別ブラウザを使うよう案内。コード側の恒久対応はしていない
  （将来的にログインをURLパラメータでなくPOST等に変更すると、同種の誤検知を避けやすくなる
  可能性がある）。
- **教訓**: 同様の「ログインだけ失敗する」相談が来たら、ネットワークやセキュリティソフトを
  疑う前に、まず①シークレットウィンドウで再現するか ②ブラウザからサインアウトすると直るか、
  の2点を最初に確認すると早い。
